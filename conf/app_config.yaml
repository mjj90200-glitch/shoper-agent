logging:
  file:
    enable: true
    level: INFO
    path: logs
    rotation: "10 MB"
    retention: "7 days"
  console:
    enable: true
    level: INFO

# 数据库与密钥类取值由 app/conf/settings.py 的 ENV_FIELDS 覆盖表从环境变量注入，
# 这里只保留本地默认值；密钥留空，缺失时由启动校验统一报错。
db_meta:
  host: localhost
  port: 3307
  user: ""
  password: ""
  database: meta

db_dw:
  host: localhost
  port: 3307
  user: ""
  password: ""
  database: dw

qdrant:
  host: localhost
  port: 6333
  embedding_size: 1024

embedding:
  host: localhost
  port: 8086
  model: BAAI/bge-large-zh-v1.5

es:
  host: localhost
  port: 9200
  index_name: data_agent

llm:
  model_name: deepseek-v4-flash
  api_key: ""
  base_url: https://api.deepseek.com

tts:
  api_key: null
  voice_type: zh_female_vv_uranus_bigtts
  base_url: https://openspeech.bytedance.com/api/v3/tts/unidirectional/sse
  resource_id: seed-tts-2.0
  timeout_seconds: 60
  max_chars: 600
