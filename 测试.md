# AI 数分助手测试需求说明

> 面向对象：功能测试、协同验收、开发联调人员  
> 测试范围：当前本地版 AI 数分助手（问数模式 + 数据分析模式）  
> 更新日期：2026-09-07

## 1. 测试目标

本轮测试需要确认以下核心目标：

1. 用户可以正常登录，且不同账号只能访问自己权限范围内的数据。
2. “问数”和“数据分析”是两个独立工作区，互相切换时内容不会丢失。
3. 问数模式能够正确处理单轮问题、多轮追问、SSE 实时进度、结果表格和图表。
4. 会话可以新建、切换、重命名和删除，刷新页面后仍能恢复。
5. 数据分析模式能够完成“目标 → 计划 → 审核 → 执行 → 图表 → 综合报告”的完整流程。
6. 分析项目可以持久化、重命名和删除，并且不同账号之间相互隔离。
7. 异常情况下页面能够给出清楚提示，不出现白屏、永久加载或误删内容。
8. 所有查询只能读取当前电商数仓，不能执行修改、删除或新增数据的 SQL。

## 2. 测试优先级

| 优先级 | 含义 | 要求 |
| --- | --- | --- |
| P0 | 核心主流程 | 必须全部通过，否则不能交付 |
| P1 | 重要功能与异常处理 | 原则上必须通过，失败需要明确评估 |
| P2 | 兼容性、体验和边界 | 建议完成，问题可进入后续优化 |

## 3. 测试环境准备

### 3.1 服务组成

项目运行时需要以下服务：

| 服务 | 地址或端口 | 正常标准 |
| --- | --- | --- |
| 前端 | `http://127.0.0.1:5173/` | 页面能够打开 |
| 后端接口文档 | `http://127.0.0.1:8000/docs` | 返回接口文档 |
| MySQL | `127.0.0.1:3307` | 容器处于运行状态 |
| Elasticsearch | `127.0.0.1:9200` | 容器处于运行状态 |
| Qdrant | `127.0.0.1:6333` | 容器处于运行状态 |
| Embedding | `127.0.0.1:8086` | 健康检查成功 |
| Kibana | `127.0.0.1:5601` | 可选，仅排查检索问题时使用 |

### 3.2 日常启动顺序

在项目根目录执行：

```powershell
docker compose -f docker/docker-compose.yaml up -d
```

启动后端：

```powershell
$env:NO_PROXY = "localhost,127.0.0.1"
$env:PYTHONUTF8 = "1"
uv run fastapi dev main.py
```

启动前端：

```powershell
cd frontend
pnpm dev
```

如果 `fastapi dev` 因 Windows 控制台编码退出，可使用：

```powershell
$env:NO_PROXY = "localhost,127.0.0.1"
$env:PYTHONUTF8 = "1"
.\.venv\Scripts\uvicorn.exe main:app --host 127.0.0.1 --port 8000
```

### 3.3 演示账号

| 账号 | 密码 | 权限预期 |
| --- | --- | --- |
| `admin` | `admin123` | 管理员，可查看全部数据和查询审计 |
| `east_manager` | `east123` | 华东区域经理，只允许查询华东数据，客户姓名脱敏 |
| `analyst` | `analyst123` | 经营分析员，可看全地区汇总，客户姓名脱敏 |

> 以上账号只用于本地测试，不应作为生产账号使用。

## 4. 测试数据与已知口径

当前数仓只包含电商销售主题：地区、客户、商品、日期和订单事实。支持验证的主要指标和维度如下：

- 指标：销售额、销量、订单数、客单价等。
- 维度：地区、省份、商品、品类、品牌、会员等级、性别、日期。
- 当前不具备：库存、成本、利润、广告、流量、竞品和外部市场数据。

当前数据快照中可用于快速核对的基准：

- 查询“统计华北地区的销售总额”，预期销售额为 `41099.5`。
- 当前地区数据中存在两个名称都为“华东”的地区记录。页面或综合报告出现两条“华东”属于已知数据质量问题，不应直接判定为前端重复渲染。
- 如果 AI 对“库存、利润、天气、编程”等非当前数仓问题生成 SQL，应判定为严重缺陷。

## 5. P0 核心验收用例

### TC-AUTH-001：管理员正常登录

1. 打开前端地址。
2. 使用 `admin / admin123` 登录。

预期：

- 登录成功并进入主工作台。
- 顶部显示当前用户信息。
- 页面没有 401、白屏或无限加载。

### TC-AUTH-002：错误密码被拒绝

1. 输入 `admin / wrong-password`。
2. 点击登录。

预期：

- 登录失败并显示明确提示。
- 不进入工作台。
- 不保存无效登录状态。

### TC-ASK-001：基础问数完整流程

1. 进入“问数”模式。
2. 输入“统计华北地区的销售总额”。
3. 点击发送。

预期：

- 页面依次显示实时流程节点，运行中的节点有明显状态。
- 最终出现结果表格，销售总额为 `41099.5`。
- 页面展示数据洞察；数据适合绘图时展示图表。
- 查询结束后状态由“运行中”变为“就绪”。
- 不出现重复提交、按钮卡死或结果缺失。

### TC-ASK-002：多轮追问理解上下文

1. 在同一会话先问“统计华东地区 2025 年第一季度的销售总额”。
2. 结果完成后追问“那华北呢？”。
3. 再追问“按品牌拆一下”。

预期：

- 第二问被补全为包含“华北、2025 年第一季度、销售额”的完整问题。
- 第三问继承正确的地区、时间和指标，并增加品牌维度。
- 每轮结果都保留在同一会话中。

### TC-ASK-003：流程图显示与隐藏

1. 发起一次问数。
2. 在输入框区域点击“隐藏流程”。
3. 查询运行期间观察简化状态。
4. 再点击“展开流程”。

预期：

- 隐藏后不再展示完整节点图。
- 查询仍继续运行，不能因为隐藏流程而停止或变慢。
- 隐藏状态下使用淡色文字显示当前流程节点，保留 SSE 实时感。
- 展开后恢复原来的节点流程图。
- 刷新页面后仍记住用户最后选择的显示状态。

### TC-SESSION-001：新建会话不删除旧会话

1. 在当前会话完成一次问数。
2. 点击“新问数”。
3. 在新会话提出另一个问题。
4. 从左侧会话列表切回旧会话。

预期：

- 新会话出现在左侧列表。
- 旧会话内容没有被清空。
- 两个会话可以独立切换。
- 当前会话名称根据首个问题自动生成。

### TC-MODE-001：问数与数据分析模式切换

1. 在问数模式保留一段对话。
2. 切换到“数据分析”。
3. 再切回“问数”。

预期：

- 桌面端可从左侧切换，窄屏或移动端可从顶部切换。
- 数据分析模式显示自己的项目列表和新建入口。
- 切回问数后，原对话和输入状态仍然存在。
- 刷新后恢复上次选中的模式。

### TC-ANALYSIS-001：完整数据分析闭环

1. 切换到“数据分析”。
2. 新建分析项目。
3. 输入目标：`分析 2025 年各地区销售表现，找出贡献最高的品类和异常月份`。
4. 点击“生成分析计划”。
5. 审核计划后点击“确认并执行”。
6. 等待全部步骤完成。

预期：

- AI 生成 2～5 个互不重复、可执行的分析步骤。
- 生成计划阶段不会直接查询数仓。
- 用户确认后才开始执行。
- 每个步骤独立显示 SSE 节点、查询结果、图表和数据明细。
- 顶部显示总体执行进度。
- 全部步骤完成后生成“综合分析报告”。
- 报告包含总体结论、关键发现、行动建议和数据说明。
- 报告数字能够在步骤结果中找到依据，不得编造数据。

### TC-ANALYSIS-002：项目持久化

1. 完成或保存一个分析项目。
2. 刷新页面。
3. 退出并重新登录同一账号。

预期：

- 项目、目标、计划、步骤状态、结果和综合报告能够恢复。
- 已完成项目不会重新自动执行。
- 如果刷新发生在执行过程中，恢复后进入可继续或重试状态，不能永久显示“运行中”。

### TC-PERM-001：区域经理数据范围

1. 使用 `east_manager / east123` 登录。
2. 查询“统计华东地区销售额”。
3. 查询“统计华北地区销售额”。
4. 查询“按大区统计全国销售额”。

预期：

- 华东查询允许执行。
- 华北和未限定华东范围的全国查询被安全拒绝。
- 页面显示可理解的权限提示，而不是泄露 SQL 或服务器堆栈。

### TC-SEC-001：只读 SQL 防护

分别尝试以下问题：

- “删除所有订单数据”。
- “把华北销售额修改成 0”。
- “执行 DROP TABLE fact_order”。

预期：

- 不执行任何写入或删除操作。
- SQL 防护拒绝非 `SELECT` 或 `WITH ... SELECT` 查询。
- 当前数仓数据保持不变。

## 6. P1 功能与异常用例

### 6.1 问数能力覆盖

以下问题每项至少测试一次：

| 编号 | 问题 | 主要检查点 |
| --- | --- | --- |
| TC-Q-01 | 按大区统计 2025 年第一季度订单数 | 地区分组、订单数 |
| TC-Q-02 | 按省份统计 2025 年第一季度销售额 | 省份维度 |
| TC-Q-03 | 统计 2025 年第一季度各商品品类销售额 | 品类维度与柱状图 |
| TC-Q-04 | 查询 2025 年第一季度销售额最高的前 5 个商品 | Top N、排序、数量恰好不超过 5 |
| TC-Q-05 | 按品牌统计华东地区 2025 年第一季度销售额 | 多条件组合 |
| TC-Q-06 | 统计 2025 年第一季度每月销售额 | 时间排序与趋势图 |
| TC-Q-07 | 统计 2025 年 3 月每天销售额 | 日粒度趋势 |
| TC-Q-08 | 按会员等级统计销售额和订单数 | 多指标结果 |
| TC-Q-09 | 统计华为品牌销售额 | 品牌值召回 |
| TC-Q-10 | 查询华北地区销售额最高的前 3 个商品 | 地区过滤 + Top N |

通用通过标准：

- SQL 使用真实表和字段，不凭空生成表名。
- SQL 自动包含外层 `LIMIT`，避免超大结果集。
- 排名问题必须有正确排序，Top N 数量符合用户要求。
- 时间趋势按真实时间顺序展示，不能按字符串错误排序。
- 表格数字、洞察文字和图表数据保持一致。

### 6.2 非数据问题边界

测试问题：

- “你好”。
- “你可以做什么？”。
- “今天天气怎么样？”。
- “帮我写一段 Python 代码”。

预期：

- 问候和能力问题返回自然语言说明。
- 天气、编程等超出当前数仓范围的问题不生成 SQL。
- 回答应引导用户询问电商销售数据。

### 6.3 会话管理

#### TC-SESSION-002：重命名

- 重命名会话后立即刷新页面。
- 预期名称保持不变，并同步到服务端。

#### TC-SESSION-003：删除

- 删除一个非当前会话，再删除当前会话。
- 预期只删除指定会话；删除当前会话后自动切换到其他会话或创建空白会话。
- 服务端删除失败时，本地内容不能被误删。

#### TC-SESSION-004：会话数量较多

- 创建至少 20 个会话。
- 预期左侧列表可滚动，主输入区不被挤压，当前会话仍清楚可见。

### 6.4 数据分析计划审核

#### TC-PLAN-001：编辑计划

1. 生成分析计划。
2. 修改步骤标题和查询问题。
3. 添加一个步骤。
4. 删除一个步骤。

预期：

- 修改结果即时保存。
- 计划至少保留 2 个步骤，最多 6 个步骤。
- 问题为空或过短时禁止执行，并给出提示。

#### TC-PLAN-002：超出数仓范围的目标

输入：`分析广告投放 ROI、库存周转和竞争对手价格`。

预期：

- 计划不能假装数仓拥有这些数据。
- 应提示当前只能分析已有电商销售数据，或把步骤限制在现有销售维度内。

#### TC-PLAN-003：计划模型不可用

1. 临时断开大模型服务或使用无效 Key。
2. 提交合法分析目标。

预期：

- 页面不白屏。
- 系统返回保守的备用分析计划，至少包含整体基线、时间趋势和关键维度拆解。

### 6.5 分析执行异常

#### TC-RUN-001：停止分析

1. 启动一个多步骤分析。
2. 执行中点击“停止分析”。

预期：

- 当前请求停止，后续步骤不再自动执行。
- 项目回到可审核或可继续状态。
- 不会一直显示加载动画。

#### TC-RUN-002：单步失败后重试

1. 让其中一个步骤产生错误，例如临时停止后端或构造无法查询的问题。
2. 点击该步骤的“单独重试”。

预期：

- 只重新执行失败步骤。
- 已成功步骤和结果保持不变。
- 重试成功后可以继续生成综合报告。

#### TC-RUN-003：跳过失败步骤

1. 在失败步骤点击“跳过”。

预期：

- 失败标记被清除，其他成功结果保留。
- 综合报告不能把被跳过的步骤描述成已获得真实结果。

#### TC-RUN-004：401 登录失效

1. 让登录令牌失效或篡改本地令牌。
2. 发起问数、生成计划或保存项目。

预期：

- 页面统一回到登录界面。
- 显示“登录状态已过期，请重新登录”。
- 原请求不会在没有权限的情况下继续执行。
- 重新登录后本地未丢失内容可以恢复。

#### TC-RUN-005：后端或 Docker 中断

分别停止后端、MySQL、Elasticsearch、Qdrant 或 Embedding 后发起请求。

预期：

- 页面显示明确错误，不白屏。
- 可以停止当前请求。
- 服务恢复后重新执行能够成功。
- 已完成的会话和项目不会因服务中断消失。

### 6.6 图表与导出

#### TC-CHART-001：图表切换

- 在同一结果中切换柱状图、趋势图和占比图。
- 预期图表即时更新，表格数据不变。

#### TC-CHART-002：指标切换

- 对同时包含销售额、销量和订单数的结果切换指标。
- 预期纵轴、提示信息和指标卡使用当前指标。

#### TC-EXPORT-001：单步 CSV

- 点击单个查询结果的“CSV”。
- 预期文件可下载、中文不乱码、列名和行数与页面一致。

#### TC-EXPORT-002：完整数据

- 在已完成分析中点击“完整数据”。
- 预期 CSV 包含所有分析步骤的数据，并能区分数据来自哪个步骤。

#### TC-EXPORT-003：打印或 PDF

- 点击“打印 / PDF”。
- 预期打开适合打印的报告页面，包含标题、目标、综合结论、关键发现、建议、限制和步骤说明。
- 使用浏览器“另存为 PDF”后，内容不能被截断或出现空白页。

## 7. P2 界面与兼容性测试

至少验证以下宽度：

- 桌面：1440 × 900。
- 小屏笔记本：1024 × 768。
- 手机：390 × 844。

检查内容：

- 桌面左侧栏正常显示；窄屏使用顶部模式切换和会话/项目抽屉。
- 长会话名称、长项目名称和长分析步骤不会破坏布局。
- 表格可横向滚动，页面主体不整体溢出。
- 图表在尺寸变化后重新适配。
- 所有图标按钮有可理解的标题或无障碍名称。
- 键盘可以聚焦输入框、发送按钮、模式切换和主要操作。
- 浏览器控制台没有未处理异常。

建议浏览器：最新版 Chrome 或 Edge。若条件允许，再验证 Firefox。

## 8. 接口测试清单

所有受保护接口在没有令牌、令牌无效或令牌过期时都应返回 `401`。

| 方法 | 接口 | 检查内容 |
| --- | --- | --- |
| POST | `/api/auth/login` | 正确登录、错误密码、返回用户权限 |
| POST | `/api/query` | SSE 格式、事件顺序、结果或错误终态 |
| GET | `/api/sessions` | 只返回当前用户会话 |
| GET | `/api/sessions/{session_id}` | 会话所有历史记录 |
| DELETE | `/api/sessions/{session_id}` | 所有权校验和删除结果 |
| GET | `/api/audits/me` | 当前用户查询审计 |
| PUT | `/api/audits/{audit_id}/feedback` | 点赞、点踩、评论和所有权 |
| GET | `/api/audits/quality-summary` | 仅管理员可查看质量汇总 |
| POST | `/api/analysis/plan` | 生成 2～5 个可执行步骤，失败时有备用计划 |
| POST | `/api/analysis/summary` | 只依据真实步骤结果生成综合报告 |
| GET | `/api/analysis/projects` | 项目列表按用户隔离 |
| PUT | `/api/analysis/projects/{project_id}` | 新建和更新项目，路径 ID 与正文 ID 必须一致 |
| DELETE | `/api/analysis/projects/{project_id}` | 只删除当前用户的指定项目 |

接口安全重点：

- 用户 A 不能读取、更新、删除用户 B 的会话、审计和分析项目。
- 项目路径 ID 与请求体 ID 不一致时返回 `400`。
- 单个分析项目超过 2 MB 时返回 `413`，不能拖垮服务。
- SSE 已经开始返回后发生异常，应通过 `type: error` 事件结束，而不是直接断开无提示。

## 9. 自动化回归

在项目根目录运行全部单元测试：

```powershell
$env:PYTHONUTF8 = "1"
uv run python -m unittest discover -s tests -v
```

前端类型检查与生产构建：

```powershell
cd frontend
pnpm build
```

问数评测集：

```powershell
uv run python -m app.scripts.evaluate_query_api --base-url http://127.0.0.1:8000
```

自动化通过标准：

- 单元测试全部通过。
- 前端 TypeScript 检查和生产构建成功。
- 评测集中的基础指标、维度、Top N、多轮追问和非数据边界用例全部有正确终态。
- 失败报告需要保留输入问题、实际 SQL、终态事件和错误信息。

## 10. 性能与稳定性观察

本项目包含多次大模型和检索调用，不使用单一固定秒数判定失败。测试时分别记录：

- 首个 SSE 进度事件耗时。
- 单次问数总耗时。
- 每个分析步骤耗时。
- 多步骤分析总耗时。
- 失败或停止后页面恢复可操作状态的耗时。

重点观察：

- 点击发送后应尽快出现状态反馈，不能长时间毫无提示。
- 前端隐藏流程不应影响后端实际查询速度。
- 同一按钮不能因连续点击产生重复请求。
- 20 个会话、20 个项目以及包含 1,000 行结果时页面仍可操作。
- 项目保存频率不能导致接口持续高频请求或页面明显卡顿。

## 11. 已知问题与判定规则

### 11.1 当前已知数据问题

当前数仓存在两个名称相同的“华东”地区记录。因此：

- 查询按地区分组时可能返回两条“华东”。
- 综合报告可能提示“华东标签重复”。
- 在数据清洗前，不应把两条“华东”销售额自行合并后当作唯一正确答案。
- 测试报告中请标记为“数据问题”，并附上 SQL 和结果，不要直接归为前端重复渲染。

### 11.2 AI 输出判定

- AI 的自然语言表述可以不同，但数字必须来自真实结果。
- 计划步骤可以不同，但必须在当前数仓范围内且可执行。
- 行动建议可以是业务推断，但必须和“事实结论”区分，不能把推断写成数据库事实。
- 模型不可用时出现备用计划属于正常降级；页面无响应或白屏不属于正常情况。

## 12. 缺陷提交模板

发现问题时请按以下格式记录：

```text
标题：<模块> 简短问题描述
优先级：P0 / P1 / P2
测试账号：
浏览器与窗口尺寸：
发生时间：

前置条件：
操作步骤：
1.
2.
3.

实际结果：
预期结果：
是否稳定复现：必现 / 偶现 / 仅一次

用户输入问题或分析目标：
会话 ID / 项目 ID：
生成的 SQL：
最后一个 SSE 节点：
接口状态码或错误信息：

附件：页面截图、控制台错误、后端日志片段
```

注意：提交截图或日志前，应删除 API Key、登录令牌和其他敏感信息。

## 13. 最终通过标准

本轮版本可以通过验收需要同时满足：

1. 所有 P0 用例通过。
2. P1 不存在数据越权、数据误写、结果数字错误、项目丢失或页面不可恢复的问题。
3. 全部自动化测试通过，前端生产构建成功。
4. 管理员、区域经理和分析员三类权限均完成验证。
5. 至少完成一次三步骤数据分析，并成功生成图表、综合报告和导出文件。
6. 服务重启、页面刷新和登录失效后，系统能够恢复到可理解、可继续操作的状态。
7. 已知“华东”重复问题在测试结果中按数据问题单独记录。
