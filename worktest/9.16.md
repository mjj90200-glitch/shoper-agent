# 9.16 测试记录：P0 收尾验证

## 测试范围

验证 asyncmy 锁版本升级后依赖安装的可复现性、SSRF 修复的功能等价性，以及全量质量门禁无回归。

## 测试结果

| # | 验证项 | 方法 | 结果 |
| --- | --- | --- | --- |
| 1 | 失败再现（修复前） | 旧锁文件全新克隆执行 `uv sync --frozen` | ✅ 稳定复现 asyncmy 0.2.11 编译失败 |
| 2 | 修复验证（一次性 venv） | `UV_PROJECT_ENVIRONMENT` 指向临时目录执行 `uv sync --frozen` | ✅ 120 包安装成功，asyncmy 0.2.14 轮子安装 |
| 3 | 修复验证（全新克隆） | 克隆新 HEAD `91ea0ec`，`uv sync --frozen` | ✅ 成功，`import asyncmy/fastapi/langgraph` 正常 |
| 4 | 前端全新安装 | 全新克隆 `pnpm install --frozen-lockfile` | ✅ 12.4s 完成，锁文件未变化 |
| 5 | URL 校验单元测试 | `python -m unittest tests.test_query_evaluator` | ✅ 8/8 通过（含 4 个新校验用例） |
| 6 | 全量后端测试 | `python -m unittest discover -s tests` | ✅ 61/61 通过 |
| 7 | 统一质量门禁 | `python -m app.scripts.quality_check` | ✅ Ruff 通过、57/57 测试、前端 tsc + Vite 构建通过 |
| 8 | 评测脚本功能冒烟 | 对运行中后端调用重构后的 `login()` | ✅ 真实返回 access_token |
| 9 | 非法地址拒绝 | `ftp://`、`file://`、无 scheme、带凭据、空主机 | ✅ 全部被 `validate_base_url` 拒绝（单元测试覆盖） |
| 10 | 安全扫描 | Mimosa 聚焦改动文件复扫 | ✅ 高危 0、中危 0；低危 2 处已修复 |

## 未覆盖与说明

- 远程 GitHub Actions CI 运行结果：需推送后查看，属于待用户执行/确认的动作。
- 评测集全量 35 轮问数回归未在本次运行（依赖大模型调用，留到 P3 评测阶段统一执行）。
- Mimosa 提示其扫描覆盖不完整，本次结论仅表明已知高危发现已清除，不等于项目整体安全。
