# 9.21 测试记录：P1-B～P1-F 验证

## 测试结果

| # | 验证项 | 方法 | 结果 |
| --- | --- | --- | --- |
| 1 | 健康检查单测（聚合/降级/超时/不泄密） | `tests.test_health_api` 6 用例，假探针注入 | ✅ 全过 |
| 2 | 健康检查真实依赖 | 启动后端请求 `/health/ready` | ✅ 五依赖 healthy，含延迟数据 |
| 3 | 依赖故障降级 | Docker 未就绪时请求 ready | ✅ 正确返回 degraded + embedding unavailable |
| 4 | 统一异常信封单测 | `tests.test_error_handling` 7 用例（404/502/500/422/request_id/SSE） | ✅ 全过 |
| 5 | 500 不泄露内部信息 | 断言响应不含异常消息中的假连接串 | ✅ |
| 6 | 迁移机制单测 | `tests.test_migrations` 4 用例（空库/幂等/旧库升级/建目录） | ✅ 全过 |
| 7 | 真实库原地迁移 | 重启后端后查 `data/shopkeeper-state.sqlite` | ✅ v1 记录在案，2 会话 + 3 审计保留 |
| 8 | 后端全量测试 | `python -m unittest discover -s tests` | ✅ 92/92 |
| 9 | 前端拆分构建 | `pnpm build`（tsc + vite） | ✅ 通过，App.tsx 247 行 |
| 10 | 前端浏览器端到端 | 登录 → 问数 → 流程图 → 洞察 41,099.5 | ✅ 与预期值一致 |
| 11 | 登录失效统一处理 | 后端重启期间旧 token 请求 401 | ✅ 前端回到登录页并提示 |
| 12 | 前端单测 | `pnpm test`（vitest） | ✅ 20/20 |
| 13 | 统一质量门禁 | `quality_check`（ruff + 后端 92 + 前端 20 + 构建） | ✅ 全绿 |

## 未覆盖与说明

- 移动端抽屉与审计面板仅构建验证，未逐一手动点击（行为搬移自原实现，逻辑未变）。
- 评测集 35 轮回归依赖大模型调用，留到 P3 评测阶段统一执行。
- 浏览器自动化工具的指针注入在本环境异常（点击超时），端到端改用页面内程序化事件完成，交互结论不受影响。
