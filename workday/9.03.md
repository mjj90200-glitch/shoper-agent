# 9 月 3 日：基础开发总结与未完成工作清单

> 今日完成了 P9 的持久化基础、P8 的质量汇总基础和 P10 的历史会话基础接口/界面。本清单保留尚未完成、待真实验证或需要从“本地演示版”升级的工作。

## 今日开发总结

- [x] 安装并验证 `langgraph-checkpoint-sqlite`，接入 `AsyncSqliteSaver`。
- [x] FastAPI 生命周期管理 SQLite Checkpointer，运行图从内存回退图切换为持久化图。
- [x] 使用 `用户名:session_id` 作为 LangGraph `thread_id`，保证用户间会话隔离。
- [x] 新增本地 SQLite 审计库：持久化查询记录、最终 SQL、行数、耗时、错误和用户反馈。
- [x] 新增会话元数据表及会话列表、详情、重命名、删除 API。
- [x] 前端新增历史会话抽屉，可切换会话、查看审计型历史摘要、继续追问、删除会话。
- [x] 管理员审计面板新增总问数、成功率、平均耗时、好评率等质量指标。
- [x] 完成 Ruff、32 项 Python 单元测试、TypeScript 检查和 Vite 生产构建。
- [ ] 尚未在真实 Docker + FastAPI 重启场景完成端到端验证。

## 一、当前阻塞项与优先级

| 优先级 | 工作 | 当前状态 | 完成标准 |
| --- | --- | --- | --- |
| P0 | Docker 真实环境验收 | Docker 暂不可启动，未验证 | MySQL、Qdrant、ES、Embedding、后端和前端全部启动，并完成 P0–P7 验收 |
| P1 | SQLite 会话持久化 | 代码完成，待真实服务重启验证 | FastAPI 重启后，同一用户同一会话仍可追问且上下文正确 |
| P1 | 审计与反馈持久化 | 代码完成，待真实服务重启验证 | 服务重启后审计记录、反馈评分和原因仍可查询 |
| P1 | P2 自动评测真实跑分 | 35 轮用例已准备，未运行 | 生成评测报告并分析失败用例与通过率 |

## 二、下一阶段功能计划

### P8：管理员质量看板

目标：把 P6 审计和 P7 反馈转为可运营的质量指标，便于答辩展示“Agent 如何持续优化”。

- [x] 管理员专属质量接口，普通用户不可访问。
- [x] 展示总问数、成功率、平均耗时、好评率。
- [ ] 展示常见失败原因与负反馈原因。
- [ ] 展示最近失败问题，支持查看对应改写问题与最终 SQL。
- [x] 前端审计面板新增管理员质量指标。
- [ ] 增加权限隔离、指标统计和空数据单元测试。

### P9：SQLite 持久化会话与审计（进行中）

目标：解决 FastAPI 重启后会话记忆、查询审计和用户反馈全部丢失的问题。

- [x] 在 `data/langgraph-checkpoints.sqlite` 定义本地 SQLite 文件位置，并加入 `.gitignore`。
- [x] 使用 `AsyncSqliteSaver` 替换 API 的 `InMemorySaver`，并在 FastAPI 生命周期中初始化/关闭。
- [x] 将 LangGraph `thread_id` 保持为 `用户 ID + session_id`，确保跨用户隔离。
- [x] 定义会话元数据表：会话 ID、用户、标题、创建/更新时间。
- [x] 将 P6 审计记录写入 `query_audit_log` 表。
- [x] 将 P7 反馈写入审计表字段。
- [ ] 增加服务重启后多轮追问、跨用户隔离、历史记录可查的集成测试。

### P10：历史会话管理

目标：从“恢复当前浏览器会话”升级为“可管理的用户会话历史”。

- [x] 会话列表 API：按当前用户查询最近会话。
- [x] 会话重命名 API；前端重命名入口待补。
- [x] 删除单个会话 API 与前端入口；Checkpointer 数据物理删除待补。
- [x] 历史详情 API，可恢复问答摘要、改写问题、SQL 与错误信息；完整结果表格与图表恢复待补。
- [x] 前端历史会话抽屉，支持切换、继续追问、删除；侧栏常驻和重命名待补。
- [ ] 处理旧会话缺失/损坏、跨用户访问、并发更新等边界。

### P11：结果分析增强

目标：增强现有确定性结果摘要和基础柱状/趋势图的表达能力。

- [ ] 多数值列时允许用户选择图表指标。
- [ ] 支持堆叠柱状图、占比图等更丰富图表。
- [ ] 在只使用真实结果数据的前提下，引入 LLM 经营洞察与下一步建议。
- [ ] 增加图表数据与结果表格一致性的自动化测试。

## 三、生产化与安全强化

### P12：SQL 执行治理

- [ ] 数据库查询超时与取消机制。
- [ ] 慢查询阈值、慢查询审计和告警字段。
- [ ] 最大扫描量/最大返回量等资源限制。
- [ ] 更严格 SQL 解析，而非仅依靠关键词和正则规则。
- [ ] 真实 MySQL 中验证危险 SQL、嵌套 SQL、异常 SQL 不会产生写入或资源滥用。

### P13：认证与数据权限生产化

- [ ] 替换内置演示账号与内存 Token。
- [ ] 接入真实认证方案（企业 SSO、JWT 或 OAuth，按部署场景选型）。
- [ ] 用户、角色、权限持久化。
- [ ] 数据库行级权限或视图级权限，避免只依赖应用层 SQL 检查。
- [ ] 登录失败限流、Token 过期与登出失效机制。
- [ ] 审计日志补充客户端、请求 ID、权限决策等字段。

## 四、测试与交付

### Docker 环境恢复后必须执行

- [ ] 按 `worktest/9.02.md` 的 B–J 完成 P0–P7 全部端到端验收。
- [ ] 运行 35 轮自动评测：`uv run python -m app.scripts.evaluate_query_api`。
- [ ] 记录通过率、失败用例、最终 SQL 与修复结果。
- [ ] 验证管理员、华东区域经理、分析员的权限差异。
- [ ] 验证图表、CSV、审计与反馈的真实数据表现。

### 工程交付与简历材料

- [ ] 建立 CI：Ruff、Python 单元测试、前端 TypeScript 与生产构建。
- [ ] 补齐环境变量模板、部署说明与故障排查文档。
- [ ] 录制或截取登录、追问、SQL 安全、图表、审计、反馈、质量看板的演示素材。
- [ ] 完善 README：二次开发说明、架构图、功能列表与运行步骤。
- [ ] 编写简历项目描述，明确“基于开源教程项目的二次工程化增强”，不表述为从零开发。

## 五、推荐执行顺序

1. Docker 环境恢复后先完成 P0–P10 的真实验收与 35 轮评测；
2. 补齐 P8 看板的失败原因、负反馈与单元测试；
3. 补齐 P10 的会话重命名 UI、Checkpointer 物理删除、完整历史结果恢复；
4. 补 P12/P13 安全生产化、CI 和部署材料；
5. 最后整理演示和简历内容。

## 六、当前已确认的依赖状态

- [x] `langgraph-checkpoint-sqlite` 已写入 `pyproject.toml`。
- [x] `AsyncSqliteSaver` 已完成本地模块导入验证。
- [x] API 运行图已在 FastAPI 生命周期中改为使用 `AsyncSqliteSaver`；独立单元测试仍保留内存回退图。
